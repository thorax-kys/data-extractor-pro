# Railway uses Nixpacks for builds
[phases.setup]
aptPkgs = ["python3", "python3-pip", "ffmpeg"]

# Install yt-dlp via pip after apt packages are available
[start]
cmd = "pip3 install --break-system-packages yt-dlp && node server.js"
