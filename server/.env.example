# Port (Railway sets this automatically)
PORT=3001

# Frontend URL for CORS (your Vercel deployment URL)
# Example: https://data-extractor.vercel.app
FRONTEND_URL=

# Downloads directory (Railway: use /tmp for ephemeral storage)
# Leave empty for default (./downloads)
DOWNLOADS_DIR=
